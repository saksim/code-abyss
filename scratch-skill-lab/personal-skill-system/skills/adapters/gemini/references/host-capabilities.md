# Host Capabilities

Gemini host adaptation should capture only the stable facts that downstream skills need.

- runtime root and discovery assumptions
- host-exposed command and skill affordances
- limits that change whether a downstream skill can execute, must translate, or must degrade
