display_name: "Workflow Template"
short_description: "Template scaffold for a workflow skill. Replace this with a workflow-specific description that says what chain it owns and when to use it."
default_prompt: "Use ~/.agents/skills/workflows/workflow-template/SKILL.md as the primary instruction source before acting on workflow-template."
