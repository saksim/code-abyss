# Failure Modes

Replace this file with the workflow's common failure patterns and recovery rules.

Include:

- where the workflow most often breaks down
- what evidence means the workflow should stop, retry, or hand off
- what operator-visible summary should be emitted when the workflow cannot complete cleanly
- which failures should trigger a skill-evolution task instead of yet another one-off patch
