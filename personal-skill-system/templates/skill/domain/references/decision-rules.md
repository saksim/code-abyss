# Decision Rules

Replace this file with the domain's default judgement rules.

Keep it compact and operational:

- what wins by default
- what tradeoffs matter most
- what anti-patterns should trigger escalation or abstention
- what should stay in this domain instead of escaping into a sibling skill
